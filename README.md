# Vue d'Ensemble - Observatoire

## Flow

- Localities
  - Each have a name, website URL, and list of URLs year by year/month by month
- Documents
  - Each has a "md5", "origin_url", "path_in_zip" (if the origin URL was a zip), "s3_path", "year", "month", "day", "hour", "ocr_json"